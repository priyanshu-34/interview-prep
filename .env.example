# Admin: email that can access the Admin panel (optional; if unset, no one is admin).
# VITE_ADMIN_EMAIL=your-email@example.com

# Optional: for Admin "Generate with AI" (create/edit question from topic name).
# Get your key from https://platform.openai.com/api-keys
# VITE_OPENAI_API_KEY=sk-...
